import LazyApp3RecurseStep from './LazyApp3RecurseStep.js';

const delayApp3RecurseStep = (left, middle, right) =>
	new LazyApp3RecurseStep(left, middle, right);

export default delayApp3RecurseStep;
